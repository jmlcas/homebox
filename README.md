# Homebox

Ver en "http://localhost:8200"

Una vez ya registrado puedes anular el registro cambiando:

 HBOX_OPTIONS_ALLOW_REGISTRATION=false     (en vez de true)

 HBOX_AUTH_API_KEY_PEPPER= Puedes modificar esta Key con el comando: openssl rand -base64 48
